<h1>Events#index</h1>
<p>Find me in app/views/events/index.html.erb</p>

<% if @events.length > 0 %>
    <ul>
        <% @events.each do |event| %>
            <li> <%= event.title %> </li>
        <% end %>
    </ul>
<% end %>