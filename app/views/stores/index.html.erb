<h1>Stores#index</h1>
<p>Find me in app/views/stores/index.html.erb</p>
<% if @stores.length > 0 %>
    <ul>
        <% @stores.each do |store| %>
            <li> <%= store.name %> </li>
        <% end %>
    </ul>
<% end %>
