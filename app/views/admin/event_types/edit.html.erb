<center>
<h4>Modifier un type d évenement: </h4>

<%= form_for @type, url: admin_event_type_path(@type.id), method: 'put' do |f| %> 
  
  
  <%if flash[:danger] %>
    <div class="alert alert-danger text-center" role="alert">
      <%=flash[:danger] %>
    </div>
  <% end %>
  <div class= 'form-group'>
    <label> Nom de l evenement : </label>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <div class='actions'>
    <%= f.submit %>
  </div>
  </center>
 

  <% end %>